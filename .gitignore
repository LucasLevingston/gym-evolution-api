# dependencies
/node_modules

# build output — not needed, API runs with tsx directly
/dist
/build

# env files
.env
.env.*
!.env.example

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo

# vercel
.vercel

# uploads (user files, not source code)
/uploads

# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
# keep .terraform.lock.hcl (like package-lock.json)
